{
  "name": "acrev360-frontend",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/portal",
    "dev:field": "npm run dev --workspace=apps/field",
    "build": "npm run build --workspace=apps/portal && npm run build --workspace=apps/field",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "codegen": "npm run codegen --workspace=packages/api",
    "e2e": "npm run e2e --workspace=apps/portal"
  },
  "engines": {
    "node": ">=20"
  }
}
